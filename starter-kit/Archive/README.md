# Archive/

Where roles go to rest — postings you passed on, applications that closed out, or searches that ended.
Move a role folder here from `../Roles/` when it's no longer active.

Nothing automated reads this folder; it's just to keep `Roles/` focused on what's live while preserving
history (and the context the prompts already accumulated from it).
